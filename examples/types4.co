
type B<U, V> {
    x: U
    y: V

    fn __add__(self: B, other: B) -> B {
        let res: B = B(self.x + other.x, self.y + other.y)
        res
    }
}

let b0: B<f64, i64> = B(1.0, 1)
let b1: B<f64, i64> = B(2.0, 2)
let b2: B = b0 + b1
