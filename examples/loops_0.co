//
// functional loop funcs
//
a := range(0, 1_000_000, 3)
    .map((n) -> { n + 1 })
    .filter((n) -> { n % 2 })
    .take_while((n) -> { n % 3 })
    .drop_while((n) -> { n % 3 })
    .reduce((acc, n) -> { acc + n }, 0)

b := range(0, 1_000_000, 3)
    .each((n) -> f64 -> { n / 2 })
