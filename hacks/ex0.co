a: u8 = 0_u8
b: u64 = 1_u64
c: i64 = 1 // i_i64

a: f32 = 0.0_f32
b: f64 = 1.0 // 1.0_f64

f := (x: f64, y: f64) -> f64 {
    r: f64 = x + y
    r
}

A := struct(
    x: f64 = 0.0,
    y: f64 = 0.0,
    
    __type__ := (
        __add__ := (self, other: Self) -> Self -> {
            Self(self.x + other.x, self.y + other.y)
        },
        
        sum := (self) -> f64 -> {
            r := self.x + self.y
        },
    )
)
